doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        link(rel="stylesheet", href="index.css")
        link(rel="stylesheet", href="repass.css")
        title Thay đổi mật khẩu đăng nhập
    body 
        div(class="card container")
            div(class="title") Thay đổi mật khẩu đăng nhập
            div(class="body")
                form(action="/auth/repass", method="post")
                    input(type="password" name="password" placeholder="Mật khẩu" class="text-input")
                    br
                    input(type="password" name="new-password" placeholder="Mật khẩu mới" class="text-input")
                    br
                    input(type="password" name="repeat-new-password" placeholder="Nhập lại mật khẩu mới" class="text-input")
                    br
                    input(type="submit" id="submit" value="Thay đổi mật khẩu" class="btn")
                p(class="failed-msg" style=`color: ${failed ? "red" : "white"}`) Mật khẩu cũ không chính xác hoặc mật khẩu mới không hợp lệ